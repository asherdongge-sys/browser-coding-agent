# GitHub OAuth App credentials. Create an OAuth App in GitHub settings.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Optional when using a non-default runtime URL.
# GITHUB_OAUTH_CALLBACK_URL=http://127.0.0.1:4317/api/github/callback
